"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/*class ControllerToken {
    
    static verifyToken(token: string){
        
        if(!token){
            return false;
        }

        if(Jwt.verify(token, process.env.SECRET || 'sadasflp-ogvoi09ia-0kv-lvo32m,=02i90k2f0-=fl0vk0-')){
            return true;
        };
    }
}

export default ControllerToken;*/
/*const ControllerToken = (req: Request, res: Response, next: NextFunction)=>{
    const authHeader = req.headers['authorization'];
    const token = authHeader && authHeader.split(' ')[1];


    try{
        

        next();
    }
    catch{
        return res.status(400).json({msg: 'INVALID TOKEN'});
    }
}

export default ControllerToken;*/ 
